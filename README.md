# dotfiles
Dev environment: vim + tmux

## installation

### install tmux&vim
ubuntu:
```bash
sudo apt install tmux vim
```
arch linux:
```bash
sudo apt install tmux vim
```

```bash
git clone https://github.com/algys/dotfiles
cd dotfiles
./install.sh
```
